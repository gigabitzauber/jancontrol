name: release-workflow
run-name: Release run ${{ github.run_number }}
on:
  workflow_dispatch:
jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-package: 'jdk'
          java-version: '25'
          check-latest: true
          cache: 'maven'

      - name: Build with Maven
        run: mvn -B clean verify -Prelease
        env:
          SIGN_KEY: ${{ secrets.GIGABITZAUBER_GPG_PRIVATE_KEY }}
          SIGN_KEY_PASS: ${{ secrets.GIGABITZAUBER_GPG_PRIVATE_KEY_PASSPHRASE }}
          # Fix warning about using sun.misc.Unsafe caused by Lombok
          MAVEN_OPTS: "--sun-misc-unsafe-memory-access=allow"

      - name: Import GPG key
        uses: crazy-max/ghaction-import-gpg@v7
        with:
          gpg_private_key: ${{ secrets.GIGABITZAUBER_GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.GIGABITZAUBER_GPG_PRIVATE_KEY_PASSPHRASE }}

      - name: Create signed hash file
        id: create_hash_file
        run: |
          HASH_FILE_NAME="jancontrol-hashes.sha256"
          echo "hash_file_name=$HASH_FILE_NAME" >> $GITHUB_OUTPUT
          cd target
          sha256sum *.jar *.asc > "${HASH_FILE_NAME}"
          gpg --batch --detach-sign --armor \
              --local-user "${SIGN_KEY_ID}" \
              --passphrase-fd 0 \
              "${HASH_FILE_NAME}" <<< "${SIGN_KEY_PASS}"
        env:
          SIGN_KEY_ID: ${{ secrets.GIGABITZAUBER_GPG_PRIVATE_KEY_ID }}
          SIGN_KEY_PASS: ${{ secrets.GIGABITZAUBER_GPG_PRIVATE_KEY_PASSPHRASE }}

      - name: Calculate version
        id: version
        run: |
          VERSION=$(bash scripts/get_current_release_version.sh)
          echo "Project version is: ${VERSION}"
          echo "jc_version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create Release Notes
        id: release_notes
        run: |
          JC_VERSION=${{ steps.version.outputs.jc_version }}
          JC_NAME="jancontrol"
          echo "jc_name=$JC_NAME" >> $GITHUB_OUTPUT
          JC_HASH_FILE_NAME=${{ steps.create_hash_file.outputs.hash_file_name }}
          RELEASE_NOTES_FILE="release_notes.md"
          echo "### Jancontrol ${JC_VERSION} Release Notes" > "${RELEASE_NOTES_FILE}"
          echo "" >> "${RELEASE_NOTES_FILE}"
          echo "Report bugs to [github.com/gigabitzauber/jancontrol/issues](https://github.com/gigabitzauber/jancontrol/issues)." >> "${RELEASE_NOTES_FILE}"
          echo "" >> "${RELEASE_NOTES_FILE}"
          echo "### Artifacts" >> "${RELEASE_NOTES_FILE}"
          echo "- \`${JC_NAME}-${JC_VERSION}.jar\` - Executable JAR" >> "${RELEASE_NOTES_FILE}"
          echo "- \`${JC_NAME}-${JC_VERSION}.jar.asc\` - Executable JAR's GPG signature" >> "${RELEASE_NOTES_FILE}"
          echo "- \`${JC_NAME}-${JC_VERSION}-sources.jar\` - Source code" >> "${RELEASE_NOTES_FILE}"
          echo "- \`${JC_NAME}-${JC_VERSION}-sources.jar.asc\` - Source code's GPG signature" >> "${RELEASE_NOTES_FILE}"
          echo "- \`${JC_HASH_FILE_NAME}\` - SHA 256 hashes for all artifacts" >> "${RELEASE_NOTES_FILE}"
          echo "- \`${JC_HASH_FILE_NAME}.asc\` - Hash file's GPG signature" >> "${RELEASE_NOTES_FILE}"
          echo "" >> "${RELEASE_NOTES_FILE}"
          echo "### Verification" >> "${RELEASE_NOTES_FILE}"
          echo "All artifacts are signed with GPG. To verify:" >> "${RELEASE_NOTES_FILE}"
          echo "\`\`\`bash" >> "${RELEASE_NOTES_FILE}"
          echo "gpg --verify <artifact>.asc" >> "${RELEASE_NOTES_FILE}"
          echo "sha256sum -c ${JC_HASH_FILE_NAME}" >> "${RELEASE_NOTES_FILE}"
          echo "\`\`\`" >> "${RELEASE_NOTES_FILE}"
          echo "" >> "${RELEASE_NOTES_FILE}"
          echo "### Run" >> "${RELEASE_NOTES_FILE}"
          echo "\`\`\`bash" >> "${RELEASE_NOTES_FILE}"
          echo "java -jar jancontrol-${JC_VERSION}.jar [options] <config-file>" >> "${RELEASE_NOTES_FILE}"
          echo "\`\`\`" >> "${RELEASE_NOTES_FILE}"

      - name: Create GitHub Release with artifacts
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.JANCONTROL_RELEASE_TOKEN }}
          files: |
            target/${{ steps.release_notes.outputs.jc_name }}-*.jar
            target/${{ steps.release_notes.outputs.jc_name }}-*.jar.asc
            target/${{ steps.release_notes.outputs.jc_name }}-*.sha256
            target/${{ steps.release_notes.outputs.jc_name }}-*.sha256.asc
          body_path: release_notes.md
          draft: false
          prerelease: false
          make_latest: true
          tag_name: v${{ steps.version.outputs.jc_version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Print Summary
        run: |
          echo "✅ Release created successfully!"
